# 源码一律 LF。
#
# 由来（2026-09-23）：仓库里 CRLF 和 LF 混着进库（core.autocrlf=false、又没有这个文件），
# 而不少测试是【扫源码做结构判断】的——review-valid.test.mjs 里一句
# src.indexOf('\n}\n', i) 在 CRLF 文件上直接返回 -1，slice(i,-1) 把整个文件后半段
# 当成了那个函数的函数体，于是别处的 rt.delete(slug) 被误判成它的，测试红在一个
# 跟真实问题完全无关的地方。换行符在这个仓库里不是审美问题。
* text=auto eol=lf

# 二进制别碰
*.png binary
*.jpg binary
*.ico binary
*.exe binary
