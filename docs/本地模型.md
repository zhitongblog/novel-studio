# 本地模型支持（写作 + 出图）

把 Novel Studio 的两条产出链路——**写正文**和**出封面底图**——都接到跑在你自己机器上的模型。

**为什么值得**：零成本、零额度、断网可写、稿子不出本机、书名实验想出几十张封面就出几十张。
**代价**：慢。12G 显卡出一章 3000 字约 2–4 分钟（云端 20–40 秒）。适合挂着长跑，不适合盯着等。

先跑一句体检，它会按你这块卡直接给出该装什么、以及照着抄的命令：

```powershell
novel local
```

---

## 一、选型结论：Qwen，不是 Gemma

你问的是 qianwen（通义千问 Qwen）还是 Gemma。**两条链路都选 Qwen，而且 Gemma 有一条根本接不上。**

### 写中文网文

| | **Qwen3 系** | Gemma 系 |
|---|---|---|
| 中文质感 | 原生中文语料，口语、成语、古白话都稳 | 多语种通才，中文有明显**翻译腔**（"我认为"而非"我觉得"，语序西化） |
| 与本项目的适配 | — | ⚠️ 冲突：`deslop.mjs` 和「反 AI 味闸」治的就是翻译腔，用 Gemma 等于自己给闸加活 |
| 许可证 | Apache 2.0 | Gemma 自有许可（有使用限制） |

网文最吃的就是**语感**，这一项 Qwen 明显强，而且它弱的地方（多语种、英文文献）本项目根本用不到。

### 出封面图

**Gemma 不能出图。** Gemma 是"看图说话"的视觉理解模型（image → text），没有任何文生图能力。
Qwen 这边有 **Qwen-Image**（20B MMDiT，Apache 2.0），中文语义理解和画面质量是目前开源最强之一。

所以出图这条链路上，Gemma 不是"差一点"，是**没有这个选项**。

---

## 二、按你的显存装哪一档

`novel local` 会自动判定。对照表：

| 显存 | 模型 | 量化 | 体积 | 说明 |
|---|---|---|---|---|
| 24G+ | `qwen3.8:27b` | Q4_K_M | ≈18GB | 27B 稠密，最接近云端 API 的质感 |
| 16–24G | `qwen3:30b-a3b` | Q4_K_M | ≈18GB | MoE 只激活 3B，速度接近 14B、质量接近 30B |
| **11–16G** | **`qwen3:14b`** | **Q4_K_M** | **≈9.3GB** | **12G 卡的甜点**：全量入显存 + 16K 上下文仍有余量 |
| 7–11G | `qwen3:8b` | Q4_K_M | ≈5.2GB | 长篇一致性弱一档，建议把每批章数调到 1 |
| <7G | `qwen3:4b` | Q4_K_M | ≈2.6GB | 只够跑通流程，长篇质量不够 |

> **别硬上大一档。** 塞不进显存不会报错，只会静默溢出到内存，速度掉 5–10 倍——
> 一章从 3 分钟变成半小时，还看不出原因。

---

## 三、装（写作侧）

```powershell
# 1) 装 Ollama
winget install Ollama.Ollama
#    或到 https://ollama.com/download 下载安装包

# 2) 拉模型（以 12G 卡为例）
ollama pull qwen3:14b

# 3) 确认
ollama list
novel local          # 应显示 ✅ 文本服务 + 已装模型
```

**模型想装到别的盘**（C 盘紧张时）：设环境变量后重启 Ollama 服务。

```powershell
[Environment]::SetEnvironmentVariable('OLLAMA_MODELS','D:\AI\ollama-models','User')
```

### ⚠️ 显存不只被权重吃掉，KV cache 是被忽略的另一半

14B 级模型每 1K 上下文的 KV cache 约 **160MB**（fp16）。16K 上下文就是 **2.6GB**——
加上 9.3GB 权重，正好把 12G 卡挤爆。Ollama 不会报错，只会**静默把一部分层放到内存**，
你只会觉得"本地模型怎么这么慢"，看不出原因。

```powershell
setx OLLAMA_FLASH_ATTENTION 1      # KV 量化的前提
setx OLLAMA_KV_CACHE_TYPE q8_0     # KV cache 减半，质量几乎无损
```

设完重启 `ollama serve`，然后跑一次写作让模型加载，再 `novel local` 看**实测**那一行。
RTX 3060 12G 上的真实结果（本项目实测）：

```
⚡ 显存实测（qwen3:14b 已加载）
   10.14G 在显存 / 0G 在内存 = 100% 上卡 —— 全部在显存 ✔
```

即 **14B + 16K 上下文在 12G 卡上是装得下的**，前提是开了上面那两个环境变量。
不开 KV 量化的话 KV cache 直接翻倍（1.14G → 2.28G），就真的会溢出。

低于 90% 就说明溢出了：把「本地上下文长度」从 16384 降到 12288，或换小一档模型。
这个数字来自 Ollama 的 `/api/ps`，是真实测量，不是估算。

### ⚠️ 国内拉不动 `ollama pull` 怎么办

Ollama 的模型 blob 放在 Cloudflare R2 上，国内常见**直连被墙 / 走代理也持续 EOF**，
表现是进度卡在某个百分比反复重试。（注意：blob 文件是**预分配**的，
用文件大小判断进度会看到虚高的数字，要看进度条本身。）

绕过办法：从**魔搭 ModelScope**（阿里自家，有 Qwen 官方仓库）下 GGUF，再导进 Ollama。

```powershell
# 1) 下载（支持 range，多线程能到 2.5MB/s；单线程只有 780KB/s）
#    https://modelscope.cn/models/Qwen/Qwen3-14B-GGUF → Qwen3-14B-Q4_K_M.gguf

# 2) 写 Modelfile（⚠️ TEMPLATE 必须写，GGUF 裸文件不带 chat 模板，
#    不写的话模型会把整段对话当续写，输出全是乱的。Qwen3 用 ChatML）
```

```dockerfile
FROM D:/AI/dl/Qwen3-14B-Q4_K_M.gguf
TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}
{{- range .Messages }}<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{ end }}<|im_start|>assistant
"""
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER num_ctx 16384
```

```powershell
ollama create qwen3:14b -f Modelfile     # 导入后与 pull 下来的完全等价
```

> ### ⚠️ 手动下载后【必须校验哈希】，别只看文件大小
>
> 多线程/断点续传下载器有一类隐蔽的坏法：某一段因内部重试重复写入了一批字节，
> 续传时又按"已有大小"算下一个起点、于是跳过头、中间**缺了一段**——
> 重复的量和缺失的量可能**刚好抵消**，最终文件大小**分毫不差**。
>
> 这时 GGUF 的文件头照样能正常解析（头部没坏），肉眼和 size 校验全都发现不了，
> 只有导入时 Ollama 报 `failed to validate GGUF with llama-quantize` 才暴露出来。
>
> ```powershell
> certutil -hashfile Qwen3-14B-Q4_K_M.gguf SHA256
> ```
>
> 跟模型页上标的 SHA256 比对。魔搭的哈希可以从这里取：
> `https://modelscope.cn/api/v1/models/<仓库>/repo/files?Revision=master&Root=`（返回 JSON 里每个文件都带 `Sha256`）。
>
> 不一致就重下。用 `curl` 手工分段时**不要用 `--retry` 配合 `>>` 追加**——
> curl 内部重试会把整个 range 从头再发一遍，正是上面那种损坏的来源。

**或者一条命令搞定**（自动按模型族补上正确的 TEMPLATE 和 stop 词）：

```powershell
novel local import --file D:\AI\dl\Qwen3-14B-Q4_K_M.gguf --name qwen3:14b
```

出图那三个模型同理：HuggingFace 直连和代理都可能不通，`hf-mirror.com` 能通但只有 ~270KB/s，
魔搭多线程能到 2.5MB/s。三个文件在魔搭的位置：

| 文件 | 魔搭仓库 |
|---|---|
| `qwen-image-2512-Q4_K_M.gguf` | `unsloth/Qwen-Image-2512-GGUF` |
| `qwen_2.5_vl_7b_fp8_scaled.safetensors` | `Comfy-Org/Qwen-Image_ComfyUI`（`split_files/text_encoders/`） |
| `qwen_image_vae.safetensors` | `Comfy-Org/Qwen-Image_ComfyUI`（`split_files/vae/`） |

### 用起来

写作台的模型下拉里选 **「本地模型（Ollama·免费·离线）」**，就跟用云端 API 一样开写。

设置 → 🖥️ 本地模型 里可调：

- **服务地址**：Ollama 默认 `http://127.0.0.1:11434/v1`；LM Studio 改 `:1234/v1`；llama.cpp server 改 `:8080/v1`
- **模型名**：要和 `ollama list` 里显示的一致
- **上下文长度 num_ctx**：默认 16384

### ⚠️ num_ctx 这个坑必须知道

Ollama 的默认上下文只有 **4096 token**，而本项目一次喂给模型的上下文包（设定圣经 + 本卷大纲 + 上一章结尾 + 章名表）通常有 **8–15K token**。

超出的部分会被**静默截断**——不报错、不警告，只是把设定和大纲悄悄砍掉，然后模型开始跑题、复读、人物崩。这是本地跑长篇最隐蔽的失败模式。

所以本项目在认出 Ollama 后，**不走它的 OpenAI 兼容端点**（那个端点不透传 `num_ctx`），改走原生 `/api/chat` 并显式传值。喂料接近上限时日志里也会给出警告。

用 LM Studio / llama.cpp 的话，请到各自界面里把上下文长度调到 16384 以上。

---

## 四、装（出图侧）

### 方案 A：SDXL 系（推荐先用这个，≈6.5GB）

**为什么够用**：本项目的封面书名是后期用 canvas 叠上去的，底图要求**干净无字**——
Qwen-Image 最强的那项能力（中文文字渲染）在这里恰好用不到，却要多占十几 G 磁盘。

```powershell
# 1) 装 ComfyUI —— Windows 建议用 portable 版（自带匹配的 Python + CUDA）：
#    https://github.com/comfyanonymous/ComfyUI/releases → ComfyUI_windows_portable_nvidia.7z
#    ⚠️ 别用系统 Python 装：Python 3.13/3.14 上 PyTorch 常常还没有对应轮子。
# 2) 下一个 SDXL 系国风大模型，放进 ComfyUI\models\checkpoints\
# 3) 启动（默认 127.0.0.1:8188）：
cd ComfyUI_windows_portable
.\python_embeded\python.exe -s ComfyUI\main.py --windows-standalone-build `
    --listen 127.0.0.1 --port 8188 --cache-none
```

> `--cache-none` 让 ComfyUI 出完图就卸载模型、不跨次累积。代价是下一张要重新读盘（多花一两分钟），
> 换来的是在 32GB 内存的机器上跑 Qwen-Image 不会把提交内存打满（见方案 B 的内存说明）。
> 只用 SDXL 的话那套模型小，可以不加。

设置 → 🎨 出图后端：后端选「本机出图」，preset 选 **SDXL 系**，
把「SDXL 大模型文件名」改成 `checkpoints\` 下的**实际文件名**（不一致是最常见的报错原因）。

### 方案 B：Qwen-Image（画面最强，≈23GB）

> **⚠️ 它吃的不只是显存，还有主机内存。**
> 13.2GB unet + 9.4GB 文本编码器，会让 ComfyUI 进程**提交约 31GB 内存**（工作集不大，但提交量吓人）。
> 32GB 内存 + Windows 默认小分页文件的机器会正好被打满，报错是
> `RuntimeError: hostbuf_file_reader_read failed` —— 字面看像 IO 错误，其实是内存不够。
>
> 启动 ComfyUI 时加 `--cache-none`（用完即卸、不跨次累积）基本能解决；
> 还紧张就调大分页文件，或把设置里的 `clipGguf` 打开改用 GGUF 编码器（9.4GB → 约 4GB）。
>
> **另外：12G 显卡上文本模型和出图模型放不下同时在显存里**（14B 占 10.1G、出图要 ~12G）。
> 本项目在本地出图前会**自动把 Ollama 的文本模型卸载**腾显存，写下一章时它自己重载（约 15 秒）。
> 不想让它自动管可以设 `image.autoUnloadText: false`。


12G 显存可跑 GGUF Q4_K_M。需要额外装 [ComfyUI-GGUF](https://github.com/city96/ComfyUI-GGUF) 自定义节点。

放三个文件（文件名要与设置里一致）：

| 文件 | 放到 |
|---|---|
| `qwen-image-2512-Q4_K_M.gguf` | `ComfyUI\models\unet\` |
| `qwen_2.5_vl_7b_fp8_scaled.safetensors` | `ComfyUI\models\text_encoders\` |
| `qwen_image_vae.safetensors` | `ComfyUI\models\vae\` |

preset 选 **Qwen-Image**。这个 preset 下出图提示词会**自动用中文**——
Qwen-Image 原生懂中文，翻成英文反而丢语义（人物气质、时代器物、国风质感这些最容易翻丢）。

### 方案 C：SD WebUI / Forge

启动参数带 `--api`，设置里「本地出图服务」选它、地址填 `http://127.0.0.1:7860`。

### 自己的工作流

在 ComfyUI 里调好后「导出(API)」成 JSON，把路径填进设置的「自定义工作流 JSON」。
里面用 `%prompt%` `%negative%` `%seed%` `%width%` `%height%` 占位，程序只替换这几个值，其余原样执行。

这条路是给「模型换代了但本项目还没更新」准备的——你不用等，自己就能救自己。

---

## 五、排错

| 现象 | 原因 / 处理 |
|---|---|
| `连不上 http://127.0.0.1:11434/v1` | Ollama 没启动。任意终端跑 `ollama serve` |
| `本地没有模型「xxx」` | `ollama pull xxx`，或把设置里的模型名改成 `ollama list` 里的 |
| 写着写着跑题、复读、人物崩 | **num_ctx 太小**，上下文被静默截断。调到 16384+ |
| 一章要跑十几分钟 | 模型塞不进显存、溢出到内存了。换小一档 |
| `ComfyUI 拒绝了这个工作流` | 设置里的模型文件名与 `models\` 下实际文件名不一致；或 Qwen-Image GGUF 缺 ComfyUI-GGUF 节点 |
| 本地地址明明能开却连不上 | **代理**。本项目对本地请求已强制直连，但若你手动配了 `api.proxy` 要留空 |
| `ollama pull` 卡在某个百分比反复重试 | Cloudflare R2 被墙。改走魔搭 + Modelfile 导入（见上） |
| 导入报 `failed to validate GGUF with llama-quantize` | 两种可能：①**磁盘空间不足**（校验要再写一份临时文件，需与模型等大的空闲空间）；②文件下坏了——**大小对不代表文件对**，用 `certutil -hashfile ... SHA256` 跟模型页比对 |
| 出图报 `hostbuf_file_reader_read failed` | **主机内存提交量不够**，不是 IO 错误。ComfyUI 加 `--cache-none`，或调大分页文件，或改用 GGUF 文本编码器 |
| 出图时显存 OOM | 文本模型还占着显存。本项目出图前会自动卸载；若你手动跑了 `ollama run` 需自己退出 |
| 章节总是写到一半就断、没有 `<<<END>>>` | 推理模型的 `<think>` 段吃掉了输出预算。本项目默认已关（`api.local.think=false`） |
| 明明显存够却很慢 | KV cache 没算进去。开 `OLLAMA_FLASH_ATTENTION=1` + `OLLAMA_KV_CACHE_TYPE=q8_0` |

> 本地请求**一律不走代理**。127.0.0.1 经 Clash 会被吞成 502 / 连接拒绝——这是本地部署第一大坑，
> 所以 `resolveProviderCfg` 对本地 provider 强制 `proxy=''`。

---

## 六、混着用最划算

本地和云端不是二选一：

- **正文长跑**用本地（量大、免费、无额度焦虑，慢一点无所谓，反正挂着跑）
- **立项 / 大纲 / 审稿 / 全文逻辑自检**用云端强模型（次数少、但最吃推理质量）
- **封面**用本地（书名实验一次出 6–8 张，云端按张计费很肉疼）

模型可以随时切换，写作规范落在每本书目录的 `AGENTS.md` / `CLAUDE.md` / `GEMINI.md` 里——**换模型不换文风**。
